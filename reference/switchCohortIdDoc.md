# Helper for consistent documentation of `switchCohortId`.

Helper for consistent documentation of `switchCohortId`.

## Arguments

- switchCohortId:

  Cohort definition IDs to use from `switchCohortTable`. If `NULL`, all
  cohort definition IDs are included.

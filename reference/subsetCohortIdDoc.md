# Helper for consistent documentation of `subsetCohortId`.

Helper for consistent documentation of `subsetCohortId`.

## Arguments

- subsetCohortId:

  Cohort definition IDs to use from `subsetCohort`.

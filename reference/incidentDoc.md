# Helper for consistent documentation of `incident`.

Helper for consistent documentation of `incident`.

## Arguments

- incident:

  Whether switch treatment must be incident, starting after
  discontinuation. If `FALSE`, it may start before discontinuation and
  continue beyond it.
